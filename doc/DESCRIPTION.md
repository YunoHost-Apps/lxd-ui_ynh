LXD UI is the official web interface for LXD: create and manage instances,
images, networks, storage and profiles, with a console on your containers and
virtual machines. LXD serves it itself, so the `lxd` app must be installed first.

**Anyone allowed here controls LXD entirely**, whatever their YunoHost account:
NGINX authenticates with a certificate that has full admin rights.
